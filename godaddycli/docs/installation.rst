============
Installation
============

At the command line::

    $ easy_install godaddycli

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv godaddycli
    $ pip install godaddycli
